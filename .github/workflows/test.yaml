name: Test Docker image

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run docker-compose
        run: |
          docker-compose build
          docker-compose up -d
          docker-compose exec git-server ls -lah repos/serve
