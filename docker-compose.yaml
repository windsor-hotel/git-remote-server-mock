version: '3.1'

services:
  git-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:80"
    environment:
      - GIT_USERNAME=dev
      - GIT_PASSWORD=dev
    volumes:
      - ./:/repos/mount/git-remote-server-mock
